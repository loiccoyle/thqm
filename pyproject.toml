[tool.poetry]
name = "thqm"
version = "0.1.0"
description = "thqm, server providing simple remote hotkey and command execution on host."
authors = ["Loic Coyle <loic.coyle@hotmail.fr>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.6"
flask = "^1.1.1"
pyqrcode = "^1.2.1"
waitress = "^1.4.3"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
